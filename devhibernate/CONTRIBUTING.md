# Contributing

Read `AGENTS.md`, the PRD, architecture, and relevant command contract before editing. Keep pull requests focused and include observable acceptance criteria, tests, manual lifecycle verification when applicable, and compatibility/data-loss analysis.

Run before submitting:

```bash
make verify
```
