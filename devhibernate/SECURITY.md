# Security Policy

DevHibernate executes repository-defined shell commands. Review `devhibernate.json` before running it. Never publish credentials, private paths, or sensitive logs in public issues. See `docs/SECURITY.md` for the threat model.
